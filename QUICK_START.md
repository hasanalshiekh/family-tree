# 📖 شرح سريع للبدء

## الخطوات السريعة

### 1️⃣ بدء التشغيل
```bash
cd "C:\Users\hasan\OneDrive\Desktop\family tree\family-tree-dashboard"
php artisan serve
```

### 2️⃣ الوصول للتطبيق
```
http://localhost:8000
```

---

## 🎯 الميزات الرئيسية

### ✨ الواجهة الرئيسية
- 📱 تصميم حديث وسريع الاستجابة
- 🌍 دعم كامل للعربية (RTL)
- 🎨 واجهة جميلة وسهلة الاستخدام

### 👨‍👩‍👧‍👦 إدارة العائلة
- ➕ إنشاء عائلات جديدة
- 👥 إضافة أعضاء العائلة
- 🔗 تحديد العلاقات (أب، أم، أبناء)
- ✏️ تعديل بيانات الأعضاء
- 🗑️ حذف الأعضاء

### 🌳 عرض الشجرة
- 📊 عرض تفاعلي لشجرة العائلة
- 🔍 تكبير وتصغير الشجرة
- 🎨 رموز ملونة لكل جيل
- 📥 تحميل الشجرة كصورة

### 📄 تصدير PDF
- 💾 حفظ الشجرة كملف PDF
- 🖨️ جاهز للطباعة
- 🌐 دعم العربية بالكامل

### 🔗 المشاركة
- 🤝 شارك شجرتك مع الأقارب
- 🔐 رابط آمن وخاص
- 👀 عرض بدون تسجيل دخول

---

## 🔌 API المتاحة

### العائلات
```
GET    /api/families              - الحصول على جميع العائلات
POST   /api/families              - إنشاء عائلة جديدة
GET    /api/families/{id}         - الحصول على عائلة معينة
PUT    /api/families/{id}         - تحديث عائلة
DELETE /api/families/{id}         - حذف عائلة
```

### أعضاء العائلة
```
GET    /api/families/{id}/members              - جميع الأعضاء
POST   /api/families/{id}/members              - إضافة عضو
GET    /api/families/{id}/members/{memberId}   - عضو معين
PUT    /api/families/{id}/members/{memberId}   - تحديث عضو
DELETE /api/families/{id}/members/{memberId}   - حذف عضو
```

### المشاركة
```
GET /api/share/{token}  - الحصول على بيانات المشاركة
```

---

## 🗄️ قاعدة البيانات

### الإعدادات الحالية
```
Host:     127.0.0.1
Port:     3306
Database: familty_tree_dashboard
Username: root
Password: root
```

### الجداول
```
- families           (العائلات)
- family_members    (أعضاء العائلة)
- migrations        (سجل الهجرات)
```

---

## 📁 الملفات المهمة

### Controllers
- `app/Http/Controllers/FamilyController.php` - إدارة العائلات
- `app/Http/Controllers/FamilyMemberController.php` - إدارة الأعضاء

### Models
- `app/Models/Family.php` - نموذج العائلة
- `app/Models/FamilyMember.php` - نموذج عضو العائلة

### Routes
- `routes/web.php` - مسارات الويب
- `routes/api.php` - مسارات API

### Views
- `resources/views/welcome.blade.php` - الصفحة الرئيسية
- `resources/views/family/` - صفحات العائلة
- `resources/views/layouts/app.blade.php` - التخطيط الأساسي

---

## ⚙️ التكوين

### ملف .env
```env
APP_NAME=Family Tree Dashboard
APP_ENV=local
APP_DEBUG=true
DB_CONNECTION=mysql
DB_DATABASE=familty_tree_dashboard
DB_USERNAME=root
DB_PASSWORD=root
```

---

## 🚀 أوامر مفيدة

### إدارة قاعدة البيانات
```bash
# تشغيل الـ migrations
php artisan migrate

# إرجاع الـ migrations
php artisan migrate:rollback

# إعادة تعيين قاعدة البيانات
php artisan migrate:fresh
```

### الأوامر الأخرى
```bash
# مسح الـ cache
php artisan cache:clear

# مسح الـ config cache
php artisan config:clear

# عرض جميع الـ routes
php artisan route:list
```

---

## 🐛 استكشاف الأخطاء

### الخادم لا يعمل
```bash
# تأكد من أن المنفذ 8000 خالي
netstat -ano | findstr :8000

# جرب منفذ آخر
php artisan serve --port=8001
```

### قاعدة البيانات لا تعمل
```bash
# تحقق من بيانات الاتصال في .env
# تأكد من أن MySQL يعمل
# شغل الـ migrations مرة أخرى
php artisan migrate:fresh --force
```

### أخطاء في الـ Views
```bash
# مسح الـ view cache
php artisan view:clear

# أعد تحميل الصفحة (Ctrl + Shift + R)
```

---

## 📞 معلومات إضافية

### الحد الأقصى للأعضاء
- النسخة الحالية: **5 أعضاء مجاناً**
- لا يوجد حد لعدد الأجيال

### لغات البرنامج
- **الواجهة:** 100% عربي
- **قاعدة البيانات:** عربي وإنجليزي
- **البرمجة:** PHP (Laravel)

### المتصفحات المدعومة
- ✅ Chrome
- ✅ Firefox
- ✅ Safari
- ✅ Edge

---

## ✅ قائمة التحقق

- ✅ Composer مثبت وتم تثبيت الحزم
- ✅ قاعدة البيانات جاهزة وتم تشغيل الـ migrations
- ✅ جميع الـ routes معرفة
- ✅ جميع الـ controllers جاهزة
- ✅ جميع الـ views موجودة
- ✅ PDF export يعمل
- ✅ API endpoints متاحة
- ✅ التطبيق يعمل على `http://localhost:8000`

---

**تم الانتهاء من المشروع بنجاح! 🎉**

جميع المشاكل تم حلها والمشروع جاهز للاستخدام الفوري.
