# 🔗 API Documentation - توثيق API

## قاعدة URL
```
http://localhost:8000/api
```

---

## 👨‍👩‍👧‍👦 Families Endpoints - نقاط نهاية العائلات

### 1. الحصول على جميع العائلات
```http
GET /api/families
```

**الوصف:** الحصول على قائمة بجميع العائلات المسجلة

**مثال الطلب:**
```bash
curl -X GET "http://localhost:8000/api/families"
```

**مثال الرد (JSON):**
```json
[
  {
    "id": 1,
    "name": "عائلة الأحمد",
    "description": "عائلة من الرياض",
    "share_token": "abc123...",
    "created_at": "2025-11-14T00:00:00.000000Z",
    "updated_at": "2025-11-14T00:00:00.000000Z"
  }
]
```

---

### 2. إنشاء عائلة جديدة
```http
POST /api/families
```

**الوصف:** إنشاء عائلة جديدة في النظام

**البيانات المطلوبة:**
```json
{
  "name": "اسم العائلة",
  "description": "وصف العائلة (اختياري)"
}
```

**مثال الطلب:**
```bash
curl -X POST "http://localhost:8000/api/families" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "عائلة محمد",
    "description": "عائلة من جدة"
  }'
```

**مثال الرد:**
```json
{
  "id": 2,
  "name": "عائلة محمد",
  "description": "عائلة من جدة",
  "share_token": "xyz789...",
  "created_at": "2025-11-14T10:30:00.000000Z",
  "updated_at": "2025-11-14T10:30:00.000000Z"
}
```

---

### 3. الحصول على عائلة معينة
```http
GET /api/families/{id}
```

**الوصف:** الحصول على تفاصيل عائلة معينة مع أعضائها

**المعاملات:**
| المعامل | النوع | الوصف |
|--------|------|-------|
| id | integer | معرف العائلة |

**مثال الطلب:**
```bash
curl -X GET "http://localhost:8000/api/families/1"
```

**مثال الرد:**
```json
{
  "id": 1,
  "name": "عائلة الأحمد",
  "description": "عائلة من الرياض",
  "share_token": "abc123...",
  "members": [
    {
      "id": 1,
      "family_id": 1,
      "name": "أحمد الأحمد",
      "arabic_name": "أحمد الأحمد",
      "gender": "male",
      "generation": 1,
      ...
    }
  ],
  "created_at": "2025-11-14T00:00:00.000000Z",
  "updated_at": "2025-11-14T00:00:00.000000Z"
}
```

---

### 4. تحديث عائلة
```http
PUT /api/families/{id}
```

**الوصف:** تحديث بيانات عائلة معينة

**المعاملات:**
| المعامل | النوع | الوصف |
|--------|------|-------|
| id | integer | معرف العائلة |

**البيانات المطلوبة:**
```json
{
  "name": "اسم العائلة الجديد",
  "description": "الوصف الجديد"
}
```

**مثال الطلب:**
```bash
curl -X PUT "http://localhost:8000/api/families/1" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "عائلة الأحمد المحدثة",
    "description": "عائلة من الرياض والدمام"
  }'
```

---

### 5. حذف عائلة
```http
DELETE /api/families/{id}
```

**الوصف:** حذف عائلة معينة وجميع أعضائها

**المعاملات:**
| المعامل | النوع | الوصف |
|--------|------|-------|
| id | integer | معرف العائلة |

**مثال الطلب:**
```bash
curl -X DELETE "http://localhost:8000/api/families/1"
```

**مثال الرد:**
```json
{
  "message": "تم حذف العائلة بنجاح"
}
```

---

## 👥 Family Members Endpoints - نقاط نهاية أعضاء العائلة

### 1. الحصول على جميع أعضاء العائلة
```http
GET /api/families/{familyId}/members
```

**الوصف:** الحصول على جميع أعضاء عائلة معينة

**المعاملات:**
| المعامل | النوع | الوصف |
|--------|------|-------|
| familyId | integer | معرف العائلة |

**مثال الطلب:**
```bash
curl -X GET "http://localhost:8000/api/families/1/members"
```

**مثال الرد:**
```json
[
  {
    "id": 1,
    "family_id": 1,
    "name": "أحمد محمد",
    "arabic_name": "أحمد محمد",
    "gender": "male",
    "birth_date": "1980-01-15",
    "death_date": null,
    "relationship": "جد",
    "father_id": null,
    "mother_id": null,
    "notes": "مؤسس العائلة",
    "generation": 1,
    "created_at": "2025-11-14T00:00:00.000000Z",
    "updated_at": "2025-11-14T00:00:00.000000Z"
  }
]
```

---

### 2. إضافة عضو جديد
```http
POST /api/families/{familyId}/members
```

**الوصف:** إضافة عضو جديد إلى العائلة

**المعاملات:**
| المعامل | النوع | الوصف |
|--------|------|-------|
| familyId | integer | معرف العائلة |

**البيانات المطلوبة:**
```json
{
  "name": "الاسم الكامل",
  "arabic_name": "الاسم بالعربية (اختياري)",
  "gender": "male|female",
  "birth_date": "YYYY-MM-DD (اختياري)",
  "death_date": "YYYY-MM-DD (اختياري)",
  "relationship": "العلاقة (اختياري)",
  "father_id": 1 (اختياري),
  "mother_id": 2 (اختياري),
  "notes": "ملاحظات (اختياري)"
}
```

**مثال الطلب:**
```bash
curl -X POST "http://localhost:8000/api/families/1/members" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "محمد أحمد",
    "arabic_name": "محمد أحمد",
    "gender": "male",
    "birth_date": "2000-05-20",
    "relationship": "ابن",
    "father_id": 1,
    "notes": "الابن الأكبر"
  }'
```

**مثال الرد:**
```json
{
  "id": 2,
  "family_id": 1,
  "name": "محمد أحمد",
  "arabic_name": "محمد أحمد",
  "gender": "male",
  "birth_date": "2000-05-20",
  "death_date": null,
  "relationship": "ابن",
  "father_id": 1,
  "mother_id": null,
  "notes": "الابن الأكبر",
  "generation": 2,
  "created_at": "2025-11-14T10:30:00.000000Z",
  "updated_at": "2025-11-14T10:30:00.000000Z"
}
```

---

### 3. الحصول على عضو معين
```http
GET /api/families/{familyId}/members/{memberId}
```

**الوصف:** الحصول على تفاصيل عضو معين

**المعاملات:**
| المعامل | النوع | الوصف |
|--------|------|-------|
| familyId | integer | معرف العائلة |
| memberId | integer | معرف العضو |

**مثال الطلب:**
```bash
curl -X GET "http://localhost:8000/api/families/1/members/1"
```

---

### 4. تحديث عضو
```http
PUT /api/families/{familyId}/members/{memberId}
```

**الوصف:** تحديث بيانات عضو معين

**المعاملات:**
| المعامل | النوع | الوصف |
|--------|------|-------|
| familyId | integer | معرف العائلة |
| memberId | integer | معرف العضو |

**البيانات (نفس بيانات الإضافة):**
```json
{
  "name": "الاسم الجديد",
  "arabic_name": "...",
  "gender": "...",
  ...
}
```

---

### 5. حذف عضو
```http
DELETE /api/families/{familyId}/members/{memberId}
```

**الوصف:** حذف عضو من العائلة

**المعاملات:**
| المعامل | النوع | الوصف |
|--------|------|-------|
| familyId | integer | معرف العائلة |
| memberId | integer | معرف العضو |

---

## 🔗 Share Endpoint - نقطة المشاركة

### الحصول على بيانات المشاركة
```http
GET /api/share/{token}
```

**الوصف:** الحصول على بيانات العائلة والأعضاء للمشاركة العامة
(لا يحتاج إلى ترخيص)

**المعاملات:**
| المعامل | النوع | الوصف |
|--------|------|-------|
| token | string | رمز المشاركة الفريد |

**مثال الطلب:**
```bash
curl -X GET "http://localhost:8000/api/share/abc123xyz789"
```

**مثال الرد:**
```json
{
  "family": {
    "id": 1,
    "name": "عائلة الأحمد",
    "description": "عائلة من الرياض",
    "share_token": "abc123xyz789",
    "created_at": "2025-11-14T00:00:00.000000Z",
    "updated_at": "2025-11-14T00:00:00.000000Z"
  },
  "members": [
    {
      "id": 1,
      "family_id": 1,
      "name": "أحمد الأحمد",
      ...
    }
  ]
}
```

---

## 📊 أمثلة استخدام Postman

### مثال 1: إنشاء عائلة وإضافة أعضاء

```
1. POST /api/families
   الرد: family_id = 1

2. POST /api/families/1/members
   (إضافة أحمد)
   الرد: member_id = 1

3. POST /api/families/1/members
   (إضافة فاطمة - الأم)
   الرد: member_id = 2

4. POST /api/families/1/members
   (إضافة محمد - الابن)
   father_id = 1, mother_id = 2
   الرد: member_id = 3

5. GET /api/families/1
   الرد: عائلة مع 3 أعضاء
```

---

## 🔐 الأمان والترخيص

### Routes المحمية
- جميع API routes الإدارية
- يمكن إضافة middleware للترخيص إذا لزم الأمر

### Routes العامة
- `GET /api/share/{token}` - للمشاركة العامة فقط

---

## 🐛 أكواد الأخطاء

| الكود | المعنى | الحل |
|------|-------|-----|
| 200 | نجح | لا توجد مشكلة |
| 201 | تم الإنشاء | العملية نجحت وتم إنشاء مورد جديد |
| 400 | طلب خاطئ | تحقق من البيانات المرسلة |
| 404 | غير موجود | تحقق من المعرف أو الرابط |
| 422 | البيانات غير صحيحة | تحقق من صيغة البيانات |
| 500 | خطأ الخادم | تحقق من سجلات الأخطاء |

---

## 📝 ملاحظات مهمة

1. **جميع التواريخ** بصيغة ISO 8601 (YYYY-MM-DD)
2. **الجنس** يجب أن يكون: `male` أو `female`
3. **رمز المشاركة** فريد لكل عائلة
4. **الجيل** يحسب تلقائياً
5. **الحد الأقصى للأعضاء** = 5 في النسخة الحالية

---

## 🚀 أمثلة كود

### JavaScript
```javascript
// الحصول على جميع العائلات
fetch('http://localhost:8000/api/families')
  .then(response => response.json())
  .then(data => console.log(data));

// إنشاء عائلة جديدة
fetch('http://localhost:8000/api/families', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    name: 'عائلة جديدة',
    description: 'وصف'
  })
})
.then(response => response.json())
.then(data => console.log(data));
```

### Python
```python
import requests

# الحصول على العائلات
response = requests.get('http://localhost:8000/api/families')
families = response.json()

# إنشاء عائلة
data = {
    'name': 'عائلة جديدة',
    'description': 'وصف'
}
response = requests.post('http://localhost:8000/api/families', json=data)
family = response.json()
```

---

**تم إنشاء التوثيق في:** November 14, 2025
